<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-3    ">
    <a class="navbar-brand" href="#">{{ 'project'|trans }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active"> <!-- TODO: Tornar `active` dinâmico -->
                <a class="nav-link" href="#">Homepage <span class="sr-only">(current)</span></a> <!-- TODO: Tornar `(current)` dinâmico -->
            </li>
        </ul>

        <div>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{ app.user.name|default(app.user) }}</a>
                    <div class="dropdown-menu pl-2" aria-labelledby="dropdown01">
                        <a href="{{ path('fos_user_security_logout') }}" class="text-danger">Logout</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
